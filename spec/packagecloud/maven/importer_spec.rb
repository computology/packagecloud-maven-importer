RSpec.describe Packagecloud::Maven::Importer do
  it "has a version number" do
    expect(Packagecloud::Maven::Importer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
